
import discord
from discord.ext import commands,tasks
from random import randrange
import asyncio
import aiohttp

with open('test.txt', 'r') as file:
   message_message = file.read().split()
token="tokenhere"
delay = 2.5
message_sender_active = False
client = commands.Bot(command_prefix='.',self_bot=True)
async def send_with_retry(ctx, message, delay):
    while message_sender_active:
        try:
            await ctx.send(message)
            break  # Message sent successfully, exit retry loop
        except discord.HTTPException:
            await asyncio.sleep(10)  # Retry after 10 seconds if there's a network error
        except discord.DiscordServerError as e:
            if e.code in (500, 502, 503, 504):
                await asyncio.sleep(10)  # Retry after 10 seconds for server errors
        except aiohttp.client_exceptions.ClientOSError as e:
            print(f"Error: {e}. Retrying in 20 seconds...")
            # Log the error to error.log for further analysis
            with open('error.log', 'a') as error_file:
                error_file.write(f"Error: {e}\n")
            await asyncio.sleep(20)  # Retry after 20 seconds for ClientOSError
            continue  # Continue to the next iteration of the loop
        except Exception as e:
            print(e)
            await asyncio.sleep(5)
            continue
async def send_messages_script(ctx,user_mentions=None):
  global message_sender_active
  if message_sender_active:
    return
  message_sender_active = True
  await ctx.message.delete()
  while message_sender_active:
    for message in message_message:
      if not message_sender_active:
        break
      if user_mentions:
        mentions = ' '.join([mention.mention for mention in user_mentions])
        await send_with_retry(ctx,f"# > {message} {mentions}",delay)
      else:
        await send_with_retry(ctx,f"# > {message}",delay)
      await asyncio.sleep(delay)
@client.event
async def on_ready():
  if not message_sender_active:
    print(f"Logged in as {client.user.name}")
    stream = discord.Streaming(
        name="Elixir🤙",
        url="https://www.twitch.tv/elixir045"
    )
    await client.change_presence(activity=stream)
@client.command()
async def start(ctx,*user_mentions:discord.User):
  global message_sender_active
  if message_sender_active:
    return
  await send_messages_script(ctx,user_mentions)
@client.command()
async def stop(ctx):
  global message_sender_active
  if message_sender_active:
    message_sender_active = False
    await ctx.message.delete()
@client.command()
async def wincondition(ctx,user_mentions):
  await ctx.message.delete()
  await ctx.send(f"# {user_mentions} = {randrange(99)}% your win condition")
@client.command()
async def muist(ctx,user_mentions):
  await ctx.message.delete()
  await ctx.send(f"# {user_mentions} = {randrange(99)}% este muist")
@client.command()
async def prost(ctx,user_mentions):
  await ctx.message.delete()
  await ctx.send(f"# {user_mentions} = {randrange(99)}% este prost futu n cur")
@client.command()
async def curva(ctx,user_mentions):
  await ctx.message.delete()
  await ctx.send(f"# {user_mentions} = {randrange(99)}% este o curva neiesita din casa ")
@client.command()
async def gay(ctx,user_mentions):
  await ctx.message.delete()
  await ctx.send(f"# {user_mentions} = {randrange(99)}% 🏳️‍🌈")
@client.command()
async def lysm(ctx, user_mentions):    
    delay = 1
    await ctx.message.delete()
    await ctx.send(f'# hey iubita 💞 {user_mentions}')
    await asyncio.sleep(delay)
    await ctx.send(f'# vreau sa știi ca te iubesc ❤ {user_mentions}')
    await asyncio.sleep(delay)
    await ctx.send(f'# pupici cu sclipici❤ {user_mentions}')
    await asyncio.sleep(delay)
    await ctx.send(f'# timpul petrecut cu tine mereu il prețuiesc si îl iubesc ❤❤ {user_mentions}')
    await asyncio.sleep(delay)
    await ctx.send(f'# de când sunt cu tine zilele mele sunt mai senine ❤❤😘{user_mentions}')
    await asyncio.sleep(delay)
    await ctx.send(f'# fără tine nu pot sa trăiesc 🥺{user_mentions}')
    await asyncio.sleep(delay)
    await ctx.send(f'# eu cu tine ma mândresc,si mereu o sa te iubesc💞❤ {user_mentions}')
    await asyncio.sleep(delay)
    await ctx.send(f'# tu esti totul meu ,totul meu🥺 {user_mentions}')
    await asyncio.sleep(delay)
    await ctx.send(f'# pana te ia Dumnezeudin bratele mele ❤😘 {user_mentions}')
    await asyncio.sleep(delay)
    await ctx.send(f'# ce te ador puisor 🥺{user_mentions}')
    await asyncio.sleep(delay)
    await ctx.send(f'# comoara mea mica ❤❤💞{user_mentions}')
@client.command()

async def dick(ctx,user_mentions):

  await ctx.message.delete()

  await ctx.send(f"# {user_mentions} are pula de {randrange(99)}cm")
@client.command()

async def call(ctx, user_mentions):    

    delay = 3

    await ctx.message.delete()

    await ctx.send(f'# calling............... {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# apel cu taxa sclavule ;))))))) {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# alooooooooooo RING RING SCLAVULE ;))))))))))))  {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# RASPUNDE SCLAVULEEEEE....... {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# RING RING FUTUTI MORTI MA TI RASPUNDE {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# ALOOOOOOOOOOOOOOOOOOO FUTUTI PE MA TA RASPUNDE TARFO {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# TARFO RASPUNDE CA MOARE MT NU POTI SCĂPA CA VIN DUPA TN SI O FUT PE MT {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# RING RING  TREZIREAAAAA FUTUTI PARASTASU MATI :))))))))))) {user_mentions}')

    await asyncio.sleep(delay) 

    await asyncio.sleep(delay) 

    await ctx.send(f'# ALOOOOOOOOOOOOOOOOOOO... TARFO RASPUNDE TI E FRICA CA TE AUDE MT CA VORBEȘTI CU TACTU LASA CA LA 3 INTRA MT IN SCHIMB PE AUTOSTRADA {user_mentions}')  
@client.command()

async def static(ctx, user_mentions):    

    delay = 3

    await ctx.message.delete()

    await ctx.send(f'# hey Static Iubirea Mea Mica❤️❤️😍😍 {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# știi ca te iubesc ❤ {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# Iubirea Mea Hai Nu Fi Supi😘😘🥰 {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# HEY STATIC STI CA ESTI Iubirea Mea Mica🤩😘😍🥰❤️ {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# HAI IUBIRE NU FI SUPA SI SUGE PULA ASTA DE BOSA❤️❤️🥰😍🤩😍❤️ {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# fără tine nu pot sa trăie{user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# eu cu tine ma mândresc,si mereu o sa te iubesc💞❤ {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# HAIDE BIBICA MEA SUGEL ❤️😍🤩🥰😘{user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# Haide in bratele mele ❤😘 {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# ce te ador puisor😘🥰🤩❤️❤️❤️😘 {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# Hey iubitamea Unde Ești 😘❤️🤩🤩❤️😍🥰 {user_mentions}')   
@client.command()

async def sclav(ctx,user_mentions):

  await ctx.message.delete()

  await ctx.send(f"# {user_mentions} Este Sclav {randrange(99)} La Suta")
@client.command()

async def muie(ctx,user_mentions):

  await ctx.message.delete()

  await ctx.send(f"# {user_mentions} A primit Muie De La {randrange(99)} DE OAMENI") 
@client.command()

async def dreptu(ctx,user_mentions):

  await ctx.message.delete()

  await ctx.send(f"# {user_mentions} SCLAVULE NAI DREPTURI UMANE {randrange(20)} LA SUTA")
@client.command()

async def dume(ctx, user_mentions):    

    delay = 3

    await ctx.message.delete()

    await ctx.send(f'# As fi putut fi tatal tau, dar tiganul din fata mea avut marunt. Eu nu.  {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# Mai face ma-ta muie cu presiune cu ciorapul pe gura? Botanista de sorta mai suge pula la colt de strada pe bilete de veteran ?  {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# Am auzit ca ma-ta se duce noaptea cand tu doarmi, vinde un kil de pizda, ma ta ia 500 g de pula shi iti da dimineata, sub forma de carnati sa mananci   {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# Sa ma cac in pizda ma-tii cu bucati de pula {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# Ma-ta e ca un congelator: Toata lumea isi pune carnea in ea. {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'# Am auzit ca sugi pula ca Eminescu fara sa-ti bata luceafara in geam {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'#  ca ma lupt stil sumo cu mata si calareste belengherul {user_mentions}')

    await asyncio.sleep(delay)

    await ctx.send(f'#  # > # ce pula mea ma, ma-ta aia nu mai are grija de mine? Ultima oara mi-a supt-o cu tot cu latei si acum nu mai vrea nici sa ma pupe pe gaoaza a dracului pretentioasa {user_mentions}')

    await asyncio.sleep(delay) 

    await asyncio.sleep(delay) 

    await ctx.send(f'# Tu ar trebui sa te numesti David Copperfield ca ai evadat din prezervativ, si nici nu ti-ai spart capul de faianta din baie {user_mentions}') 
client.run(token, bot=False) ianta din baie {user_mentions}') 
client.run(token, bot=False) 